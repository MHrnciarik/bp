<div class="mb-6">
  <%= link_to "← Back to Invoice", invoice_path(@invoice), class: "text-lavender hover:text-lavender-dark text-sm transition-colors" %>
</div>

<h1 class="text-3xl font-bold text-mint mb-6">Edit Invoice</h1>
<%= render "form" %>